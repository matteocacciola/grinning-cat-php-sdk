services:
  php:
    container_name: grinning-cat-php-sdk
    build:
      context: ./docker/php
      args:
        - UID=${UID}
        - GID=${GID}
    ports:
      - "8082:8081"
    volumes:
      - ./:/var/www/grinning-cat-php-sdk
